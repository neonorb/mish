object Iterator<Item> {
	permission HasNext {
		authorize Iterator
	}
	
	def Function<Boolean, Void>(
		if @.in {
			grants: HasNext
		} else {
			revokes: HasNext
		}
	) hasNext
	
	def Function<Item, Void>(
		invoke: HasNext
	) next
}